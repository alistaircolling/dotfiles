Run `gh pr list` in the current repository to list open pull requests.

If the user provided an argument: "$ARGUMENTS", use it as a search query by passing it to `gh pr list --search "$ARGUMENTS"`.

If "$ARGUMENTS" is empty, just run `gh pr list` with no search filter.

Display the results to the user.
