---
description: Create a new git worktree for parallel development
---
Create a new git worktree with an isolated session for parallel development.

Run the worktree shell function with the user's argument:

```
wt $ARGUMENTS
```

If "$ARGUMENTS" is empty, run `wt` with no arguments (it will list existing worktrees).
